import { SortByTimeFieldPipe } from './sort-by-time-field.pipe';

describe('SortByTimeFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTimeFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
