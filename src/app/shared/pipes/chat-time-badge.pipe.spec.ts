import { ChatTimeBadgePipe } from './chat-time-badge.pipe';

describe('ChatTimeBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new ChatTimeBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
